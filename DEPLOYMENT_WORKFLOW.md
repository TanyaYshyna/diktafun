# 🚀 Workflow для разработки и деплоя

## 📋 Стратегия работы с ветками

### Ветки:
- **`main`** — продакшн (то, что работает на сайте)
- **`develop`** — разработка (где ты работаешь в течение дня)

### 🔄 Ежедневный workflow

#### В течение дня (разработка):
```bash
# 1. Переключись на ветку разработки
git checkout develop

# 2. Работай, делай изменения, коммить сколько хочешь
git add .
git commit -m "Описание изменений"
git push origin develop

# Можно повторять сколько угодно раз в день
```

#### Ночью (деплой на продакшн):
```bash
# 1. Убедись, что все изменения закоммичены в develop
git checkout develop
git status  # проверь, что нет незакоммиченных изменений

# 2. Обнови develop из GitHub (на случай, если работала с другого компьютера)
git pull origin develop

# 3. Переключись на main и обнови её
git checkout main
git pull origin main

# 4. Смержи develop в main
git merge develop

# 5. Запушь main — Railway автоматически задеплоит
git push origin main

# 6. Вернись на develop для следующего дня
git checkout develop
```

## 🎯 Быстрый деплой (скрипт)

Создан скрипт `deploy_to_production.sh` для автоматизации ночного деплоя.

## ⚠️ Важные правила

1. **НИКОГДА не коммить напрямую в `main`** — только через merge из `develop`
2. **Всегда проверяй `git status`** перед деплоем
3. **Деплой только ночью**, когда пользователи не активны
4. Если что-то пошло не так — можно откатиться через `git revert`

## 🔧 Настройка Railway

В настройках Railway должен быть включен автодеплой из ветки `main`:
- Settings → Source → Branch: `main`
- Auto Deploy: включено

## 📝 Первоначальная настройка (один раз)

```bash
# 1. Создать ветку develop из текущего main
git checkout -b develop

# 2. Запушить develop в GitHub
git push -u origin develop

# 3. Вернуться на main
git checkout main
```

