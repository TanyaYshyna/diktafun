# 🌐 Настройка домена dictafan.com для Railway

## 📋 Что нужно знать

### 1. Про точку в конце адреса в Railway

**Это нормально!** Точка в конце адреса (например, `uftno6f1.up.railway.app.`) — это стандартная запись DNS для корневого домена. Railway показывает её правильно. Проблема была не в этом.

### 2. Правильный адрес Railway

В Railway есть два типа адресов:
- **Временный адрес**: `uftno6f1.up.railway.app` (меняется при каждом деплое)
- **Постоянный адрес**: `dictafan-production.up.railway.app` (если настроен custom domain)

**Для настройки DNS нужно использовать тот адрес, который Railway показывает в настройках домена!**

---

## 🎯 Вариант 1: Настройка через Cloudflare (РЕКОМЕНДУЕТСЯ)

Cloudflare — это не только хранилище! Это отличный DNS-провайдер, который:
- ✅ Бесплатный
- ✅ Быстрый
- ✅ Защищает от DDoS-атак
- ✅ Может кэшировать статику (но это можно отключить)
- ✅ Не мешает использовать Backblaze B2 для аудио

### Шаг 1: Перенести домен в Cloudflare

1. Зайди на [developers.cloudflare.com](https://developers.cloudflare.com) или [dash.cloudflare.com](https://dash.cloudflare.com)
2. Зарегистрируйся (если ещё нет аккаунта)
3. Нажми **"Add a Site"** или **"Добавить сайт"**
4. Введи `dictafan.com`
5. Выбери бесплатный план (Free)
6. Cloudflare покажет тебе **nameservers** (серверы имён), например:
   - `alice.ns.cloudflare.com`
   - `bob.ns.cloudflare.com`

### Шаг 2: Настроить nameservers в Namecheap

1. Зайди на [www.namecheap.com](https://www.namecheap.com)
2. Зайди в **Domain List** → выбери `dictafan.com`
3. Перейди в **Advanced DNS** или **Nameservers**
4. Выбери **Custom DNS** (вместо Namecheap BasicDNS)
5. Введи nameservers от Cloudflare (те, что показал Cloudflare на шаге 1)
6. Сохрани

**Важно:** Изменения могут занять до 24 часов, но обычно работают через 1-2 часа.

### Шаг 3: Настроить DNS записи в Cloudflare

1. Вернись в Cloudflare Dashboard
2. Выбери домен `dictafan.com`
3. Перейди в **DNS** → **Records**

#### Найди правильный адрес Railway:

1. Зайди в Railway Dashboard
2. Выбери свой проект
3. Перейди в **Settings** → **Domains** (или **Custom Domain**)
4. Там будет показан адрес, который нужно использовать

**Варианты:**

**Если Railway показывает адрес типа `uftno6f1.up.railway.app`:**
- Тип: `CNAME`
- Имя: `@` (это означает корневой домен `dictafan.com`)
- Цель/Content: `uftno6f1.up.railway.app`
- Proxy status: **DNS only** (серый облачко) или **Proxied** (оранжевое облачко) — попробуй оба варианта
- TTL: Auto

**Если Railway показывает адрес типа `dictafan-production.up.railway.app`:**
- Тип: `CNAME`
- Имя: `@`
- Цель/Content: `dictafan-production.up.railway.app`
- Proxy status: **DNS only** или **Proxied**
- TTL: Auto

**Если Railway требует A-запись (IP-адрес):**
- Тип: `A`
- Имя: `@`
- IPv4 address: (IP-адрес, который даст Railway)
- Proxy status: **DNS only** или **Proxied**
- TTL: Auto

### Шаг 4: Настроить домен в Railway

1. В Railway Dashboard → Settings → Domains
2. Добавь custom domain: `dictafan.com`
3. Railway проверит DNS-записи и подтвердит подключение

---

## 🎯 Вариант 2: Настройка напрямую через Namecheap

Если не хочешь использовать Cloudflare, можно настроить DNS прямо в Namecheap:

### Шаг 1: Найти адрес Railway

1. Railway Dashboard → Settings → Domains
2. Посмотри, какой адрес там указан

### Шаг 2: Настроить DNS в Namecheap

1. Зайди на namecheap.com
2. Domain List → `dictafan.com` → **Advanced DNS**

#### Если Railway даёт CNAME адрес:

- Тип: `CNAME Record`
- Host: `@`
- Value: `uftno6f1.up.railway.app` (или тот, что показал Railway)
- TTL: Automatic

#### Если Railway даёт IP-адрес:

- Тип: `A Record`
- Host: `@`
- Value: (IP-адрес от Railway)
- TTL: Automatic

### Шаг 3: Настроить домен в Railway

1. Railway Dashboard → Settings → Domains
2. Добавь `dictafan.com`

---

## ❓ Частые проблемы

### Проблема: "DNS records not found" в Railway

**Решение:**
1. Проверь, что DNS-записи сохранены правильно
2. Подожди 1-2 часа (DNS распространяется не мгновенно)
3. Проверь DNS через [whatsmydns.net](https://www.whatsmydns.net) — введи `dictafan.com` и посмотри, что показывают серверы по всему миру

### Проблема: Сайт не открывается

**Решение:**
1. Проверь, что домен добавлен в Railway
2. Проверь, что Railway показывает статус "Active" для домена
3. Попробуй открыть сайт через `https://dictafan.com` (не `http://`)
4. Очисти кэш браузера (Ctrl+Shift+Delete)

### Проблема: SSL сертификат не работает

**Решение:**
- Railway автоматически выдаёт SSL-сертификат через Let's Encrypt
- Это может занять до 10 минут после настройки DNS
- Если не работает через час, проверь DNS-записи ещё раз

---

## 🔍 Как проверить, что всё работает

1. **Проверь DNS:**
   ```bash
   # В терминале (Mac/Linux):
   dig dictafan.com
   # или
   nslookup dictafan.com
   ```

2. **Проверь онлайн:**
   - [whatsmydns.net](https://www.whatsmydns.net) — проверка DNS по всему миру
   - [dnschecker.org](https://dnschecker.org) — ещё один инструмент проверки

3. **Проверь в браузере:**
   - Открой `https://dictafan.com`
   - Должен открыться твой сайт
   - В адресной строке должен быть замочек 🔒 (SSL работает)

---

## 💡 Важные замечания

1. **Cloudflare и Backblaze B2:** Можно использовать одновременно!
   - Cloudflare для DNS (управление доменом)
   - Backblaze B2 для хранения аудиофайлов
   - Они не мешают друг другу

2. **Proxy в Cloudflare:**
   - **Proxied** (оранжевое облачко) = Cloudflare кэширует и защищает
   - **DNS only** (серое облачко) = Cloudflare только управляет DNS
   - Для начала попробуй **DNS only**, если не работает — переключи на **Proxied**

3. **Время распространения DNS:**
   - Обычно: 1-2 часа
   - Максимум: 24-48 часов
   - Не паникуй, если сразу не работает!

---

## 📝 Чек-лист настройки

- [ ] Домен перенесён в Cloudflare (или настроен DNS в Namecheap)
- [ ] Nameservers обновлены в Namecheap
- [ ] DNS-запись (CNAME или A) создана правильно
- [ ] Домен добавлен в Railway
- [ ] Railway показывает статус "Active" для домена
- [ ] Сайт открывается по адресу `https://dictafan.com`
- [ ] SSL-сертификат работает (замочек в браузере)

---

## 🆘 Если ничего не помогает

1. Проверь логи в Railway Dashboard
2. Убедись, что приложение работает на Railway (проверь временный адрес)
3. Попробуй удалить домен в Railway и добавить заново
4. Проверь, что DNS-записи правильные через [whatsmydns.net](https://www.whatsmydns.net)

