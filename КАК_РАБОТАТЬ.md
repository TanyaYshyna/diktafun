# 📖 Простая инструкция: как работать с ветками

## 🎯 Главное правило

- **`main`** = то, что работает на сайте (продакшн)
- **`develop`** = где ты работаешь в течение дня (разработка)

## 🌞 В течение дня (когда работаешь)

### 1. Убедись, что ты на ветке `develop`:
```bash
git checkout develop
```

### 2. Делай свои изменения в коде

### 3. Сохраняй изменения в Git:
```bash
git add .
git commit -m "Описание того, что изменила"
git push origin develop
```

**Можешь повторять шаги 2-3 сколько угодно раз в день!** Это не повлияет на сайт.

---

## 🌙 Ночью (когда хочешь обновить сайт)

### Вариант 1: Запустить скрипт (проще всего) ✨

1. Открой терминал в Cursor (View → Terminal или `Ctrl+``)
2. Убедись, что ты в папке проекта:
   ```bash
   cd /Users/tanyayushyna/Documents/python_pr/dictafan
   ```
3. Запусти скрипт:
   ```bash
   ./deploy_to_production.sh
   ```

Скрипт сам всё сделает:
- Проверит, что нет незакоммиченных изменений
- Обновит ветки из GitHub
- Смержит `develop` в `main`
- Запустит деплой на Railway

### Вариант 2: Вручную (если скрипт не работает)

```bash
# 1. Переключись на develop
git checkout develop

# 2. Убедись, что всё закоммичено
git status

# 3. Обнови develop
git pull origin develop

# 4. Переключись на main
git checkout main

# 5. Обнови main
git pull origin main

# 6. Смержи develop в main
git merge develop

# 7. Запушь main (Railway задеплоит автоматически)
git push origin main

# 8. Вернись на develop
git checkout develop
```

---

## ❓ Частые вопросы

### Как проверить, на какой ветке я сейчас?
```bash
git branch
```
Звёздочка (*) показывает текущую ветку.

### Что если я случайно закоммитила в `main`?
Не страшно! Просто переключись на `develop` и продолжай работать там:
```bash
git checkout develop
```

### Как отменить последний коммит (если ещё не запушила)?
```bash
git reset --soft HEAD~1
```

### Скрипт не запускается, пишет "Permission denied"
Сделай скрипт исполняемым:
```bash
chmod +x deploy_to_production.sh
```

---

## 🎓 Запомни

- **Днём** → работай в `develop`, коммить сколько хочешь
- **Ночью** → запусти скрипт `./deploy_to_production.sh`
- **Никогда не коммить напрямую в `main`** (только через merge из `develop`)

